From 022b6d42a6a7d08984f9720812d3fc62eefbc363 Mon Sep 17 00:00:00 2001
From: Wilson Zeng <cheng20011202@gmail.com>
Date: Tue, 16 Jun 2026 21:49:57 +0800
Subject: [PATCH 3/6] mm/slub: bypass per-CPU sheaves for SLUB_TINY

---
 mm/slub.c | 36 +++++++++++++++++++++++++-----------
 1 file changed, 26 insertions(+), 10 deletions(-)

--- a/mm/slub.c
+++ b/mm/slub.c
@@ -4937,6 +4937,9 @@
 	bool node_requested;
 	void *object;
 
+	if (IS_ENABLED(CONFIG_SLUB_TINY))
+		return NULL;
+
 #ifdef CONFIG_NUMA
 	if (static_branch_unlikely(&strict_numa) &&
 			 node == NUMA_NO_NODE) {
@@ -5231,7 +5234,8 @@
 	if (unlikely(!size))
 		return NULL;
 
-	if (unlikely(size > s->sheaf_capacity)) {
+	if (IS_ENABLED(CONFIG_SLUB_TINY) ||
+	    unlikely(size > s->sheaf_capacity)) {
 
 		sheaf = kzalloc_flex(*sheaf, objects, size, gfp);
 		if (!sheaf)
@@ -5312,7 +5321,8 @@
 	struct slub_percpu_sheaves *pcs;
 	struct node_barn *barn;
 
-	if (unlikely((sheaf->capacity != s->sheaf_capacity)
+	if (IS_ENABLED(CONFIG_SLUB_TINY) ||
+	    unlikely((sheaf->capacity != s->sheaf_capacity)
 		     || sheaf->pfmemalloc)) {
 		sheaf_flush_unused(s, sheaf);
 		kfree(sheaf);
@@ -5378,7 +5393,8 @@
 		return 0;
 
 	if (likely(sheaf->capacity >= size)) {
-		if (likely(sheaf->capacity == s->sheaf_capacity))
+		if (!IS_ENABLED(CONFIG_SLUB_TINY) &&
+		    likely(sheaf->capacity == s->sheaf_capacity))
 			return __prefill_sheaf_pfmemalloc(s, sheaf, gfp);
 
 		if (!__kmem_cache_alloc_bulk(s, gfp, sheaf->capacity - sheaf->size,
@@ -6029,6 +6050,9 @@
 {
 	struct slub_percpu_sheaves *pcs;
 
+	if (IS_ENABLED(CONFIG_SLUB_TINY))
+		return false;
+
 	if (!local_trylock(&s->cpu_sheaves->lock))
 		return false;
 
@@ -6128,6 +6152,9 @@
 	struct slub_percpu_sheaves *pcs;
 	struct slab_sheaf *rcu_sheaf;
 
+	if (IS_ENABLED(CONFIG_SLUB_TINY))
+		return false;
+
 	if (WARN_ON_ONCE(IS_ENABLED(CONFIG_PREEMPT_RT)))
 		return false;
 
@@ -8741,10 +8768,12 @@
 	s->min_partial = min_t(unsigned long, MAX_PARTIAL, ilog2(s->size) / 2);
 	s->min_partial = max_t(unsigned long, MIN_PARTIAL, s->min_partial);
 
-	s->cpu_sheaves = alloc_percpu(struct slub_percpu_sheaves);
-	if (!s->cpu_sheaves) {
-		err = -ENOMEM;
-		goto out;
+	if (!IS_ENABLED(CONFIG_SLUB_TINY)) {
+		s->cpu_sheaves = alloc_percpu(struct slub_percpu_sheaves);
+		if (!s->cpu_sheaves) {
+			err = -ENOMEM;
+			goto out;
+		}
 	}
 
 #ifdef CONFIG_NUMA
@@ -8765,9 +8794,11 @@
 		goto out;
 #endif
 
-	err = init_percpu_sheaves(s);
-	if (err)
-		goto out;
+	if (!IS_ENABLED(CONFIG_SLUB_TINY)) {
+		err = init_percpu_sheaves(s);
+		if (err)
+			goto out;
+	}
 
 	err = 0;
 
-- 
2.54.0

