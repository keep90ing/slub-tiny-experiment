diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/arch/arm/boot/dts/st/stm32f429-disco.dts ../buildroot-2026.02/output/build/linux-7.0/arch/arm/boot/dts/st/stm32f429-disco.dts
--- linux-7.0/arch/arm/boot/dts/st/stm32f429-disco.dts	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/arch/arm/boot/dts/st/stm32f429-disco.dts	2026-04-20 02:00:07.947702192 +0800
@@ -58,7 +58,7 @@
 	compatible = "st,stm32f429i-disco", "st,stm32f429";
 
 	chosen {
-		bootargs = "root=/dev/ram";
+		bootargs = "root=mtd:romfs rootfstype=romfs ro";
 		stdout-path = "serial0:115200n8";
 	};
 
@@ -67,6 +67,18 @@
 		reg = <0x90000000 0x800000>;
 	};
 
+	flash@8000000 {
+		compatible = "mtd-rom";
+		reg = <0x8000000 0x200000>;
+		bank-width = <4>;
+		#address-cells = <1>;
+
+		partition@150000 {
+			label = "romfs";
+			reg = <0x150000 0xb0000>;
+		};
+	};
+
 	aliases {
 		serial0 = &usart1;
 	};
@@ -109,12 +121,31 @@
 		regulator-name = "vcc5_host1";
 		regulator-always-on;
 	};
+
+	v3v3: regulator-v3v3 {
+		compatible = "regulator-fixed";
+		regulator-name = "v3v3";
+		regulator-min-microvolt = <3300000>;
+		regulator-max-microvolt = <3300000>;
+		regulator-always-on;  
+	};
 };
 
 &clk_hse {
 	clock-frequency = <8000000>;
 };
 
+&sdio {
+	status = "okay";
+	pinctrl-names = "default", "opendrain";
+	pinctrl-0 = <&sdio_pins>;
+	pinctrl-1 = <&sdio_pins_od>;
+	bus-width = <1>;
+	max-frequency = <1000000>;
+	broken-cd;
+	vmmc-supply = <&v3v3>;
+};
+
 &crc {
 	status = "okay";
 };
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/arch/arm/Kconfig ../buildroot-2026.02/output/build/linux-7.0/arch/arm/Kconfig
--- linux-7.0/arch/arm/Kconfig	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/arch/arm/Kconfig	2026-04-20 02:00:07.948372658 +0800
@@ -3,6 +3,7 @@
 	bool
 	default y
 	select ARCH_32BIT_OFF_T
+    select ARCH_SUPPORTS_LTO_GCC if LD_IS_BFD
 	select ARCH_CORRECT_STACKTRACE_ON_KRETPROBE if HAVE_KRETPROBES && FRAME_POINTER && !ARM_UNWIND
 	select ARCH_HAS_BINFMT_FLAT
 	select ARCH_HAS_CACHE_LINE_SIZE if OF
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/arch/arm/mm/Makefile ../buildroot-2026.02/output/build/linux-7.0/arch/arm/mm/Makefile
--- linux-7.0/arch/arm/mm/Makefile	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/arch/arm/mm/Makefile	2026-04-20 02:00:07.949260826 +0800
@@ -44,6 +44,7 @@
 obj-$(CONFIG_CPU_CACHE_FA)	+= cache-fa.o
 obj-$(CONFIG_CPU_CACHE_NOP)	+= cache-nop.o
 obj-$(CONFIG_CPU_CACHE_V7M)	+= cache-v7m.o
+CFLAGS_cache.o += $(DISABLE_LTO_GCC)
 obj-y				+= cache.o
 
 obj-$(CONFIG_CPU_COPY_V4WT)	+= copypage-v4wt.o
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/arch/Kconfig ../buildroot-2026.02/output/build/linux-7.0/arch/Kconfig
--- linux-7.0/arch/Kconfig	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/arch/Kconfig	2026-04-20 02:00:07.949372670 +0800
@@ -799,6 +799,12 @@
 	  An architecture should select this option if it can support Clang's
 	  ThinLTO mode.
 
+config ARCH_SUPPORTS_LTO_GCC
+    bool
+
+config BAD_AR
+	bool
+
 config HAS_LTO_CLANG
 	def_bool y
 	depends on CC_IS_CLANG && LD_IS_LLD && AS_IS_LLVM
@@ -824,6 +830,18 @@
 	  If unsure, select LTO_NONE. Note that LTO is very resource-intensive
 	  so it's disabled by default.
 
+config LTO_GCC
+	bool "GCC Link Time Optimization (LTO) (EXPERIMENTAL)"
+	depends on ARCH_SUPPORTS_LTO_GCC
+	depends on CC_IS_GCC
+	depends on !FTRACE_MCOUNT_USE_RECORDMCOUNT
+	depends on !KASAN
+	depends on !GCOV_KERNEL
+	select LTO
+	help
+	  This option enables Link Time Optimization (LTO) for the kernel
+	  using the GCC toolchain.
+
 config LTO_NONE
 	bool "None"
 	help
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/drivers/char/hw_random/stm32-rng.c ../buildroot-2026.02/output/build/linux-7.0/drivers/char/hw_random/stm32-rng.c
--- linux-7.0/drivers/char/hw_random/stm32-rng.c	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/drivers/char/hw_random/stm32-rng.c	2026-04-20 02:00:07.950345125 +0800
@@ -363,11 +363,20 @@
 		return -EINVAL;
 	}
 
-	clk_bulk_disable_unprepare(priv->data->nb_clock, priv->clk_bulk);
+    if (IS_ENABLED(CONFIG_PM))
+        clk_bulk_disable_unprepare(priv->data->nb_clock, priv->clk_bulk);
 
 	return 0;
 }
 
+static void stm32_rng_cleanup(struct hwrng *rng)
+{
+		struct stm32_rng_private *priv =
+			container_of(rng, struct stm32_rng_private, rng);
+		if (!IS_ENABLED(CONFIG_PM))
+		clk_bulk_disable_unprepare(priv->data->nb_clock, priv->clk_bulk);
+}
+
 static void stm32_rng_remove(struct platform_device *ofdev)
 {
 	pm_runtime_disable(&ofdev->dev);
@@ -563,6 +572,7 @@
 
 	priv->rng.name = dev_driver_string(dev);
 	priv->rng.init = stm32_rng_init;
+    priv->rng.cleanup = stm32_rng_cleanup;
 	priv->rng.read = stm32_rng_read;
 	priv->rng.quality = 900;
 
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/drivers/gpu/drm/stm/ltdc.c ../buildroot-2026.02/output/build/linux-7.0/drivers/gpu/drm/stm/ltdc.c
--- linux-7.0/drivers/gpu/drm/stm/ltdc.c	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/drivers/gpu/drm/stm/ltdc.c	2026-04-20 02:00:07.950863192 +0800
@@ -2080,14 +2080,16 @@
 		goto err;
 	}
 
-	clk_disable_unprepare(ldev->pixel_clk);
+	if (IS_ENABLED(CONFIG_PM)) {
+		clk_disable_unprepare(ldev->pixel_clk);
 
-	if (ldev->bus_clk)
-		clk_disable_unprepare(ldev->bus_clk);
+		if (ldev->bus_clk)
+			clk_disable_unprepare(ldev->bus_clk);
 
-	pinctrl_pm_select_sleep_state(ddev->dev);
+			pinctrl_pm_select_sleep_state(ddev->dev);
 
-	pm_runtime_enable(ddev->dev);
+		pm_runtime_enable(ddev->dev);
+    }
 
 	return 0;
 err:
@@ -2101,9 +2103,16 @@
 
 void ltdc_unload(struct drm_device *ddev)
 {
+	struct ltdc_device *ldev = ddev->dev_private;
 	drm_dbg_driver(ddev, "\n");
 
-	pm_runtime_disable(ddev->dev);
+	if (IS_ENABLED(CONFIG_PM))
+		pm_runtime_disable(ddev->dev);
+	else {
+		clk_disable_unprepare(ldev->pixel_clk);
+		if (ldev->bus_clk)
+			clk_disable_unprepare(ldev->bus_clk);
+	}
 }
 
 MODULE_AUTHOR("Philippe Cornu <philippe.cornu@st.com>");
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/include/linux/compiler_attributes.h ../buildroot-2026.02/output/build/linux-7.0/include/linux/compiler_attributes.h
--- linux-7.0/include/linux/compiler_attributes.h	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/include/linux/compiler_attributes.h	2026-04-20 02:00:07.951372695 +0800
@@ -220,9 +220,21 @@
  * gcc: https://gcc.gnu.org/onlinedocs/gcc/Common-Function-Attributes.html#Common-Function-Attributes
  * clang: https://clang.llvm.org/docs/AttributeReference.html#flatten
  */
+#ifdef CONFIG_LTO_GCC
+# define __flatten
+#else
 # define __flatten			__attribute__((flatten))
+#endif
 
 /*
+ * Optional: __noreorder to prevent GCC from reordering functions
+ */
+#ifdef CONFIG_LTO_GCC
+# define __noreorder			__attribute__((no_reorder))
+#else
+# define __noreorder
+#endif
+/*
  * Note the missing underscores.
  *
  *   gcc: https://gcc.gnu.org/onlinedocs/gcc/Common-Function-Attributes.html#index-noinline-function-attribute
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/include/linux/compiler.h ../buildroot-2026.02/output/build/linux-7.0/include/linux/compiler.h
--- linux-7.0/include/linux/compiler.h	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/include/linux/compiler.h	2026-04-20 02:00:07.952030169 +0800
@@ -369,6 +369,14 @@
  */
 #define prevent_tail_call_optimization()	mb()
 
+#ifdef CONFIG_LTO_GCC
+#define __visible_on_lto __visible
+#define __global_on_lto  "globl"
+#else
+#define __visible_on_lto
+#define __global_on_lto  "local"
+#endif
+
 #include <asm/rwonce.h>
 
 #endif /* __LINUX_COMPILER_H */
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/include/linux/init.h ../buildroot-2026.02/output/build/linux-7.0/include/linux/init.h
--- linux-7.0/include/linux/init.h	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/include/linux/init.h	2026-04-20 02:00:07.952371184 +0800
@@ -261,7 +261,7 @@
 	static_assert(__same_type(initcall_t, &fn));
 #else
 #define ____define_initcall(fn, __unused, __name, __sec)	\
-	static initcall_t __name __used 			\
+	static initcall_t __name __used __noreorder		\
 		__attribute__((__section__(__sec))) = fn;
 #endif
 
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/include/linux/linkage.h ../buildroot-2026.02/output/build/linux-7.0/include/linux/linkage.h
--- linux-7.0/include/linux/linkage.h	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/include/linux/linkage.h	2026-04-20 02:00:07.952709337 +0800
@@ -26,6 +26,9 @@
 #define cond_syscall(x)	asm(				\
 	".weak " __stringify(x) "\n\t"			\
 	".set  " __stringify(x) ","			\
+		 __stringify(sys_ni_syscall) "\n\t"	\
+	".weak " __stringify(compat_##x) "\n\t"		\
+	".set  " __stringify(compat_##x) ","		\
 		 __stringify(sys_ni_syscall))
 #endif
 
@@ -33,7 +36,10 @@
 #define SYSCALL_ALIAS(alias, name) asm(			\
 	".globl " __stringify(alias) "\n\t"		\
 	".set   " __stringify(alias) ","		\
-		  __stringify(name))
+		  __stringify(name) "\n\t"		\
+	".globl " __stringify(compat_##alias) "\n\t"	\
+	".set   " __stringify(compat_##alias) ","	\
+		  __stringify(compat_##name))
 #endif
 
 #define __page_aligned_data	__section(".data..page_aligned") __aligned(PAGE_SIZE)
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/init/Makefile ../buildroot-2026.02/output/build/linux-7.0/init/Makefile
--- linux-7.0/init/Makefile	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/init/Makefile	2026-04-20 02:00:07.952997889 +0800
@@ -60,3 +60,4 @@
 
 $(obj)/version-timestamp.o: include/generated/utsversion.h
 CFLAGS_version-timestamp.o := -include include/generated/utsversion.h
+CFLAGS_version-timestamp.o += $(DISABLE_LTO_GCC)
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/Kbuild ../buildroot-2026.02/output/build/linux-7.0/Kbuild
--- linux-7.0/Kbuild	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/Kbuild	2026-04-20 02:00:07.953157740 +0800
@@ -10,6 +10,7 @@
 bounds-file := include/generated/bounds.h
 
 targets := kernel/bounds.s
+KBUILD_CFLAGS += $(DISABLE_LTO_GCC)
 
 $(bounds-file): kernel/bounds.s FORCE
 	$(call filechk,offsets,__LINUX_BOUNDS_H__)
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/kernel/entry/common.c ../buildroot-2026.02/output/build/linux-7.0/kernel/entry/common.c
--- linux-7.0/kernel/entry/common.c	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/kernel/entry/common.c	2026-04-20 02:02:29.652099994 +0800
@@ -185,7 +185,7 @@
 static inline bool arch_irqentry_exit_need_resched(void) { return true; }
 #endif
 
-void raw_irqentry_exit_cond_resched(void)
+__visible void raw_irqentry_exit_cond_resched(void)
 {
 	if (!preempt_count()) {
 		/* Sanity check RCU and thread stack */
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/kernel/livepatch/Kconfig ../buildroot-2026.02/output/build/linux-7.0/kernel/livepatch/Kconfig
--- linux-7.0/kernel/livepatch/Kconfig	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/kernel/livepatch/Kconfig	2026-04-20 02:00:07.953587884 +0800
@@ -11,6 +11,7 @@
 	depends on SYSFS
 	depends on KALLSYMS_ALL
 	depends on HAVE_LIVEPATCH
+	depends on !LTO_GCC
 	depends on !TRIM_UNUSED_KSYMS
 	help
 	  Say Y here if you want to support kernel live patching.
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/kernel/softirq.c ../buildroot-2026.02/output/build/linux-7.0/kernel/softirq.c
--- linux-7.0/kernel/softirq.c	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/kernel/softirq.c	2026-04-20 02:00:07.953820833 +0800
@@ -659,7 +659,7 @@
 /**
  * irq_enter_rcu - Enter an interrupt context with RCU watching
  */
-void irq_enter_rcu(void)
+__visible void irq_enter_rcu(void)
 {
 	__irq_enter_raw();
 
@@ -734,10 +734,10 @@
  *
  * Also processes softirqs if needed and possible.
  */
-void irq_exit_rcu(void)
+__visible void irq_exit_rcu(void)
 {
 	__irq_exit_rcu();
-	 /* must be last! */
+	/* must be last! */
 	lockdep_hardirq_exit();
 }
 
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/kernel/static_call.c ../buildroot-2026.02/output/build/linux-7.0/kernel/static_call.c
--- linux-7.0/kernel/static_call.c	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/kernel/static_call.c	2026-04-20 02:00:07.954321430 +0800
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-2.0
 #include <linux/static_call.h>
 
-long __static_call_return0(void)
+__visible long __static_call_return0(void)
 {
 	return 0;
 }
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/lib/Makefile ../buildroot-2026.02/output/build/linux-7.0/lib/Makefile
--- linux-7.0/lib/Makefile	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/lib/Makefile	2026-04-20 02:00:07.954569775 +0800
@@ -31,6 +31,7 @@
 KASAN_SANITIZE_string.o := n
 
 CFLAGS_string.o += -fno-stack-protector
+CFLAGS_string.o += $(DISABLE_LTO_GCC)
 endif
 
 lib-y := ctype.o string.o vsprintf.o cmdline.o \
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/Makefile ../buildroot-2026.02/output/build/linux-7.0/Makefile
--- linux-7.0/Makefile	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/Makefile	2026-04-20 02:00:07.955388168 +0800
@@ -1044,12 +1044,15 @@
 KBUILD_LDFLAGS += -mllvm -import-instr-limit=5
 endif
 
-ifdef CONFIG_LTO
+ifdef CONFIG_LTO_CLANG
 KBUILD_CFLAGS	+= -fno-lto $(CC_FLAGS_LTO)
 KBUILD_AFLAGS	+= -fno-lto
 export CC_FLAGS_LTO
 endif
 
+LDFINAL ?= $(LD)
+export LDFINAL
+
 ifdef CONFIG_CFI
 CC_FLAGS_CFI	:= -fsanitize=kcfi
 ifdef CONFIG_CFI_ICALL_NORMALIZE_INTEGERS
@@ -1134,6 +1137,7 @@
 include-$(CONFIG_PROPELLER_CLANG)	+= scripts/Makefile.propeller
 include-$(CONFIG_WARN_CONTEXT_ANALYSIS) += scripts/Makefile.context-analysis
 include-$(CONFIG_GCC_PLUGINS)	+= scripts/Makefile.gcc-plugins
+include-$(CONFIG_LTO_GCC)	+= scripts/Makefile.lto
 
 include $(addprefix $(srctree)/, $(include-y))
 
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/mm/kasan/generic.c ../buildroot-2026.02/output/build/linux-7.0/mm/kasan/generic.c
--- linux-7.0/mm/kasan/generic.c	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/mm/kasan/generic.c	2026-04-20 02:00:07.956681275 +0800
@@ -165,15 +165,15 @@
 		case 16:
 			return memory_is_poisoned_16(addr);
 		default:
-			BUILD_BUG();
+			break;
 		}
 	}
 
 	return memory_is_poisoned_n(addr, size);
 }
 
-static __always_inline bool check_region_inline(const void *addr,
-						size_t size, bool write,
+static __always_inline bool check_region_inline(const void *addr, size_t size,
+						bool write,
 						unsigned long ret_ip)
 {
 	if (!kasan_enabled())
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/mm/kasan/shadow.c ../buildroot-2026.02/output/build/linux-7.0/mm/kasan/shadow.c
--- linux-7.0/mm/kasan/shadow.c	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/mm/kasan/shadow.c	2026-04-20 02:00:07.957200192 +0800
@@ -48,7 +48,7 @@
  * these, so that non-instrumented files can safely consider them as builtins.
  */
 #undef memset
-void *memset(void *addr, int c, size_t len)
+void __used *memset(void *addr, int c, size_t len)
 {
 	if (!kasan_check_range(addr, len, true, _RET_IP_))
 		return NULL;
@@ -58,7 +58,7 @@
 
 #ifdef __HAVE_ARCH_MEMMOVE
 #undef memmove
-void *memmove(void *dest, const void *src, size_t len)
+void __used *memmove(void *dest, const void *src, size_t len)
 {
 	if (!kasan_check_range(src, len, false, _RET_IP_) ||
 	    !kasan_check_range(dest, len, true, _RET_IP_))
@@ -69,7 +69,7 @@
 #endif
 
 #undef memcpy
-void *memcpy(void *dest, const void *src, size_t len)
+void __used *memcpy(void *dest, const void *src, size_t len)
 {
 	if (!kasan_check_range(src, len, false, _RET_IP_) ||
 	    !kasan_check_range(dest, len, true, _RET_IP_))
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/bloat-o-meter ../buildroot-2026.02/output/build/linux-7.0/scripts/bloat-o-meter
--- linux-7.0/scripts/bloat-o-meter	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/bloat-o-meter	2026-04-20 02:00:07.957700935 +0800
@@ -24,6 +24,7 @@
 args = parser.parse_args()
 
 re_NUMBER = re.compile(r'\.[0-9]+')
+re_LTO = re.compile(r'\.lto_priv\.[0-9]+')
 
 def getsizes(file, format):
     sym = {}
@@ -46,6 +47,7 @@
                 if name == "linux_banner": continue
                 if name == "vermagic": continue
                 # statics and some other optimizations adds random .NUMBER
+                name = re_LTO.sub('', name)
                 name = re_NUMBER.sub('', name)
                 sym[name] = sym.get(name, 0) + int(size, 16)
     return sym
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/gcc-ld ../buildroot-2026.02/output/build/linux-7.0/scripts/gcc-ld
--- linux-7.0/scripts/gcc-ld	1970-01-01 08:00:00.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/gcc-ld	2026-04-20 02:00:07.957928031 +0800
@@ -0,0 +1,40 @@
+#!/bin/sh
+# SPDX-License-Identifier: GPL-2.0
+# run gcc with ld options
+# used as a wrapper to execute link time optimizations
+# yes virginia, this is not pretty
+
+ARGS="-nostdlib"
+
+for j in "$@" ; do
+	if [ "$j" = -v ] ; then
+		exec `$CC -print-prog-name=ld` -v
+	fi
+done
+
+while [ "$1" != "" ] ; do
+	case "$1" in
+	-save-temps*|-m32|-m64) N="$1" ;;
+	-r) N="$1" ;;
+	-flinker-output*) N="$1" ;;
+	-[Wg]*) N="$1" ;;
+	-[olv]|-[Ofd]*|-nostdlib) N="$1" ;;
+	--end-group|--start-group|--whole-archive|--no-whole-archive|\
+--no-undefined|--hash-style*|--build-id*|--eh-frame-hdr|-Bsymbolic)
+		 N="-Wl,$1" ;;
+	-[RTFGhIezcbyYu]*|\
+--script|--defsym|-init|-Map|--oformat|-rpath|\
+-rpath-link|--sort-section|--section-start|-Tbss|-Tdata|-Ttext|-soname|\
+--version-script|--dynamic-list|--version-exports-symbol|--wrap|-m|-z)
+		A="$1" ; shift ; N="-Wl,$A,$1" ;;
+	-[m]*) N="$1" ;;
+	-*) N="-Wl,$1" ;;
+	*)  N="$1" ;;
+	esac
+	ARGS="$ARGS $N"
+	shift
+done
+
+[ -n "$V" ] && echo >&2 $CC $ARGS
+
+exec $CC $ARGS
\ No newline at end of file
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/link-vmlinux.sh ../buildroot-2026.02/output/build/linux-7.0/scripts/link-vmlinux.sh
--- linux-7.0/scripts/link-vmlinux.sh	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/link-vmlinux.sh	2026-04-20 02:00:07.958123584 +0800
@@ -32,6 +32,7 @@
 KBUILD_LDFLAGS="$2"
 LDFLAGS_vmlinux="$3"
 VMLINUX="$4"
+LDFINAL="$5"
 
 is_enabled() {
 	grep -q "^$1=y" include/config/auto.conf
@@ -60,7 +61,7 @@
 	# skip output file argument
 	shift
 
-	if is_enabled CONFIG_LTO_CLANG || is_enabled CONFIG_X86_KERNEL_IBT ||
+	if is_enabled CONFIG_LTO_CLANG || is_enabled CONFIG_X86_KERNEL_IBT || is_enabled CONFIG_LTO_GCC ||
 	   is_enabled CONFIG_KLP_BUILD; then
 		# Use vmlinux.o instead of performing the slow LTO link again.
 		objs=vmlinux.o
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/Makefile.build ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.build
--- linux-7.0/scripts/Makefile.build	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.build	2026-04-20 02:00:07.958372781 +0800
@@ -494,7 +494,7 @@
 	$(call if_changed,ar)
 
 quiet_cmd_ld_multi_m = LD [M]  $@
-      cmd_ld_multi_m = $(LD) $(ld_flags) -r -o $@ @$< $(cmd_objtool)
+      cmd_ld_multi_m = $(LDFINAL) $(ld_flags) -r -o $@ @$< $(cmd_objtool)
 
 define rule_ld_multi_m
 	$(call cmd_and_savecmd,ld_multi_m)
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/Makefile.lib ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.lib
--- linux-7.0/scripts/Makefile.lib	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.lib	2026-04-20 02:00:07.959036546 +0800
@@ -207,7 +207,7 @@
 	$(if $(delay-objtool), --link)					\
 	$(if $(part-of-module), --module)
 
-delay-objtool := $(or $(CONFIG_LTO_CLANG),$(CONFIG_X86_KERNEL_IBT),$(CONFIG_KLP_BUILD))
+delay-objtool := $(or $(CONFIG_LTO),$(CONFIG_X86_KERNEL_IBT),$(CONFIG_KLP_BUILD))
 
 cmd_objtool = $(if $(objtool-enabled), ; $(objtool) $(objtool-args) $@)
 cmd_gen_objtooldep = $(if $(objtool-enabled), { echo ; echo '$@: $$(wildcard $(objtool))' ; } >> $(dot-target).cmd)
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/Makefile.lto ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.lto
--- linux-7.0/scripts/Makefile.lto	1970-01-01 08:00:00.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.lto	2026-04-20 02:00:07.959423334 +0800
@@ -0,0 +1,43 @@
+#
+# Support for gcc link time optimization
+#
+
+DISABLE_LTO_GCC :=
+export DISABLE_LTO_GCC
+
+ifdef CONFIG_LTO_GCC
+	CC_FLAGS_LTO_GCC := -flto
+	DISABLE_LTO_GCC := -fno-lto
+
+	KBUILD_CFLAGS += ${CC_FLAGS_LTO_GCC}
+
+	CC_FLAGS_LTO := -flto
+	export CC_FLAGS_LTO
+
+	lto-flags-y := -flinker-output=nolto-rel -flto=jobserver
+	lto-flags-y += -fwhole-program
+
+	lto-flags-$(CONFIG_LTO_CP_CLONE) += -fipa-cp-clone
+
+	# allow extra flags from command line
+	lto-flags-y += ${LTO_EXTRA_CFLAGS}
+
+	# For LTO we need to use gcc to do the linking, not ld
+	# directly. Use a wrapper to convert the ld command line
+	# to gcc
+	LDFINAL := ${CONFIG_SHELL} ${srctree}/scripts/gcc-ld \
+                  ${lto-flags-y}
+
+	# LTO gcc creates a lot of files in TMPDIR, and with /tmp as tmpfs
+	# it's easy to drive the machine OOM. Use the object directory
+	# instead for temporaries.
+	# This has the drawback that there might be some junk more visible
+	# after interrupted compilations, but you would have that junk
+	# there anyways in /tmp.
+	TMPDIR ?= $(objtree)
+	export TMPDIR
+
+	# use plugin aware tools
+	AR = $(CROSS_COMPILE)gcc-ar
+	NM = $(CROSS_COMPILE)gcc-nm
+endif # CONFIG_LTO_GCC
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/Makefile.modfinal ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.modfinal
--- linux-7.0/scripts/Makefile.modfinal	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.modfinal	2026-04-20 02:00:07.959603973 +0800
@@ -34,7 +34,7 @@
 
 quiet_cmd_ld_ko_o = LD [M]  $@
       cmd_ld_ko_o =							\
-	$(LD) -r $(KBUILD_LDFLAGS)					\
+	$(LDFINAL) -r $(KBUILD_LDFLAGS)					\
 		$(KBUILD_LDFLAGS_MODULE) $(LDFLAGS_MODULE)		\
 		-T $(objtree)/scripts/module.lds -o $@ $(filter %.o, $^)
 
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/Makefile.vmlinux ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.vmlinux
--- linux-7.0/scripts/Makefile.vmlinux	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.vmlinux	2026-04-20 02:00:07.959786097 +0800
@@ -64,9 +64,11 @@
 
 # Final link of vmlinux with optional arch pass after final link
 cmd_link_vmlinux =							\
-	$< "$(LD)" "$(KBUILD_LDFLAGS)" "$(LDFLAGS_vmlinux)" "$@";	\
+	$< "$(LD)" "$(KBUILD_LDFLAGS)" "$(LDFLAGS_vmlinux)" "$@" "$(LDFINAL)";	\
 	$(if $(ARCH_POSTLINK), $(MAKE) -f $(ARCH_POSTLINK) $@, true)
 
+CFLAGS_.vmlinux.export.o += $(DISABLE_LTO_GCC)
+
 targets += vmlinux.unstripped .vmlinux.export.o
 vmlinux.unstripped: scripts/link-vmlinux.sh vmlinux.o .vmlinux.export.o $(KBUILD_LDS) FORCE
 	+$(call if_changed_dep,link_vmlinux)
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/Makefile.vmlinux_o ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.vmlinux_o
--- linux-7.0/scripts/Makefile.vmlinux_o	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/Makefile.vmlinux_o	2026-04-20 02:00:07.960033699 +0800
@@ -56,7 +56,7 @@
 
 quiet_cmd_ld_vmlinux.o = LD      $@
       cmd_ld_vmlinux.o = \
-	$(LD) ${KBUILD_LDFLAGS} -r -o $@ \
+	$(LDFINAL) ${KBUILD_LDFLAGS} -r -o $@ \
 	$(vmlinux-o-ld-args-y) \
 	$(addprefix -T , $(initcalls-lds)) \
 	--whole-archive vmlinux.a --no-whole-archive \
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/mksysmap ../buildroot-2026.02/output/build/linux-7.0/scripts/mksysmap
--- linux-7.0/scripts/mksysmap	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/mksysmap	2026-04-20 02:00:07.960167425 +0800
@@ -92,3 +92,6 @@
 # ppc stub
 /\.long_branch\./d
 /\.plt_branch\./d
+
+# GCC LTO .c. weak symbols
+/ \.c\./d
diff -urN -x '.files-list-*' -x '.applied*' -x .applied_patches_list -x .br_regen_dot_config -x '*include-prefixes*' -x '*.files-list.before*' linux-7.0/scripts/mod/Makefile ../buildroot-2026.02/output/build/linux-7.0/scripts/mod/Makefile
--- linux-7.0/scripts/mod/Makefile	2026-04-13 04:48:06.000000000 +0800
+++ ../buildroot-2026.02/output/build/linux-7.0/scripts/mod/Makefile	2026-04-20 02:00:07.960330402 +0800
@@ -1,5 +1,6 @@
 # SPDX-License-Identifier: GPL-2.0
 CFLAGS_REMOVE_empty.o += $(CC_FLAGS_LTO)
+CFLAGS_empty.o += $(DISABLE_LTO_GCC)
 
 hostprogs-always-y	+= modpost mk_elfconfig
 always-y		+= empty.o
@@ -12,6 +13,7 @@
 	$(call filechk,offsets,__DEVICETABLE_OFFSETS_H__)
 
 targets += $(devicetable-offsets-file) devicetable-offsets.s
+KBUILD_CFLAGS += $(DISABLE_LTO_GCC)
 
 # dependencies on generated files need to be listed explicitly
 
